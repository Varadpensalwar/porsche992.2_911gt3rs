import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.porsche911gt3rs.app',
  appName: 'porsche911gt3rs',
  webDir: 'build'
};

export default config;
